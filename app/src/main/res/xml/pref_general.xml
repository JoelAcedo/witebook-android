<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_max_stories_saved_key"
        android:title="@string/pref_max_stories_saved_label"
        android:defaultValue="@string/pref_max_stories_saved_default"
        android:entries="@array/pref_max_stories_saved_titles"
        android:entryValues="@array/pref_max_stories_saved_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
